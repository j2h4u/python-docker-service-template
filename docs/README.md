# Documentation Map

This directory separates documentation by audience and lifecycle.

- `template/` contains reusable guidance that is intentionally copied into
  repositories created from this template.
- `meta/` contains maintainer documentation for this template repository
  itself. GitHub templates copy it mechanically, but new projects should delete
  or rewrite it.

